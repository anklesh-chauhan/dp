# DocuPharma product modularization roadmap

## Verified checkpoint

- Module enum, dependency rules, configuration, manager, exception, and `module:*` middleware exist.
- DMS protects the current Filament panel and document print route.
- AI observability, template controls, actions, and queued jobs are entitlement-aware.
- DMS-only template creation does not dispatch AI generation.
- Database seeding is module-aware.
- Core, DMS, AI, and QMS module seeders own their permission boundaries and grants.
- The legacy `SopDocument` model implements the DMS `ControlledDocument` contract without persistence renames.
- Document revision uses canonical DMS action and service namespaces with legacy compatibility entry points.
- Document issuance, recall, and controlled-copy destruction use canonical DMS namespaces with legacy compatibility entry points.
- Document and template retention lifecycle actions use canonical DMS namespaces with legacy compatibility entry points.
- Document locking uses canonical DMS namespaces across Filament and workflow consumers with legacy compatibility entry points.
- Template publishing uses canonical DMS namespaces with legacy compatibility entry points.
- Controlled-document generation uses canonical DMS namespaces for SOP and log creation with legacy compatibility entry points.
- Document numbering, SOP-reference resolution, and template-variable resolution use canonical DMS services with legacy compatibility entry points.
- Audit logging uses a canonical Shared service across DMS and workflow consumers with the legacy SOP entry point and persistence retained.
- Document activation uses the canonical DMS service from approval workflows with the legacy SOP entry point retained.
- `SopDocument` implements the Shared `ApprovableSubject` contract for reusable workflow identity and attribution without approval persistence changes.
- Workflow submission authorization consumes `ApprovableSubject`, allowing non-DMS subjects to use the existing permission and ownership rules without changing SOP approval persistence.
- Workflow selection consumes `ApprovableSubject` department metadata while retaining `SopWorkflow` as the existing persistence adapter and preserving department-specific precedence with global fallback.
- Shared workflow-definition and selector contracts now sit in front of the DMS `SopWorkflow` selection adapter, with the existing SOP engine return type and approval persistence retained.
- Workflow execution consumes Shared workflow-definition and step identity metadata while retaining `SopApproval` rows, status transitions, and submitted audit payloads.
- Approval-instance initialization is behind a Shared persistence contract, with the DMS adapter retaining `SopApproval` creation, resubmission reset behavior, and foreign keys.
- `SopApproval` implements the Shared approval-instance contract for reusable subject, workflow-step, decision, attribution, timestamp, and signature metadata without persistence changes.
- Approval decision entry points consume the Shared approval-instance contract, with an explicit SOP adapter guard retaining existing authorization and persistence behavior.
- Approval decision mutations are behind a Shared persistence contract, with the DMS adapter retaining `SopApproval` decision lookups, attribution, timestamps, comments, and signature persistence.
- Approval decision authorization is behind a Shared contract, with the DMS adapter retaining SOP permission, role, department, mandatory-step ordering, and separation-of-duties rules and the model method retained as a compatibility entry point.
- Approval decision outcomes are behind a Shared contract, with the DMS adapter retaining SOP status transitions, audit payloads, mandatory-completion checks, final activation, unlocking, and transactional rollback behavior.
- Workflow submission lifecycle handling is behind a Shared contract, with the DMS adapter retaining SOP draft validation, unlocking, under-review status, submitted audit payloads, and transactional rollback behavior.
- Workflow submission authorization is behind a Shared contract, with the DMS adapter retaining SOP permissions, administrator and maker roles, department scope, creator and owner rules, and the engine compatibility entry point.
- Workflow decision execution consumes and returns Shared approval instances without an engine-level `SopApproval` guard, while DMS adapters retain type validation and existing SOP actions retain their Filament-compatible return behavior.
- Workflow orchestration uses Shared approval decision-code vocabulary while retaining the existing DMS lookup codes, rows, foreign keys, model constants, and string adapter boundaries.
- Canonical workflow-engine ownership is in the Shared domain, with the legacy SOP service retained as a compatibility subclass and all product behavior supplied through Shared contracts and DMS adapters.
- Shared electronic-signature metadata now covers meaning, signer, timestamp, hash, reason, IP address, and user agent; `SopApproval` adapts its existing decision, approver, approval timestamp, signature hash, and comments while returning null for metadata not present in the existing approval schema.
- SOP approval decisions now persist request IP address and user agent through the Shared decision-persistence boundary, reset that context on workflow reinitialization, and expose it through the Shared electronic-signature metadata contract.
- Approved SOP decisions now use a module-neutral Shared hashing contract with deterministic SHA-256 hashing over canonical record identity, meaning, signer, UTC timestamp, reason, IP address, and user-agent metadata, and persisted approvals can reproduce their stored hash.
- Approved, rejected, and returned SOP decisions now share the same atomic canonical-signature path while retaining existing authorization, DMS action return types, status transitions, audit payloads, and transactional rollback behavior.
- Shared electronic-signature integrity verification now reconstructs canonical metadata hashes and uses timing-safe comparison; persisted approved, rejected, and returned SOP decisions adapt to this reusable verification boundary.
- The primary Filament dashboard and all DMS metrics now declare DMS entitlement ownership, while document-control, issuance, and knowledge navigation use explicit DMS group labels that leave room for future QMS presentation.
- Filament settings now separate Core organization and identity navigation from DMS configuration; the DMS settings cluster has explicit entitlement access and existing panel middleware continues to block disabled-module direct routes.
- Knowledge guides now carry explicit product-module ownership, existing content defaults to DMS, and Filament queries, filters, and management choices are constrained to enabled entitlements so future QMS guidance can reuse the library without leaking across module boundaries.
- Reporting widgets now declare their owning product module through one reusable Filament concern; all DMS dashboard metrics and AI performance metrics enforce their own entitlement directly, leaving future QMS reporting independent of DMS presentation wiring.
- Dormant product-license persistence now stores issuer key identity, exact signed payloads, detached signatures, and lifecycle timestamps; a contract-bound OpenSSL RSA/SHA-256 verifier rejects tampering, malformed signatures, and unknown keys while environment configuration remains the active entitlement source.
- Signed-license activation now verifies issuer authenticity before validating versioned UUID, module, issue, expiry, and grace claims; invalid dependency sets and malformed lifecycles leave no persisted license, while valid claims derive stored lifecycle timestamps.
- Product-license lifecycle evaluation now re-verifies the stored signature and classifies exact active, grace, expired, revoked, and invalid states, including expiry/grace boundaries and scheduled revocation timing, without changing live entitlements.
- `ModuleManager` now consumes a module-entitlement provider contract; the bound configuration provider preserves `DOCUPHARMA_MODULES` normalization and dependency behavior exactly while allowing a signed-license provider to be introduced without changing consumers.
- An unbound signed-license entitlement provider now selects the newest verified active or grace-period license with valid dependency-complete module claims, falls back to an older usable license when appropriate, and returns no entitlements for invalid, revoked, expired, malformed, unknown, or duplicate claims.
- Entitlement sourcing is now explicitly configurable: environment mode remains the default, signed-license mode is opt-in, unknown sources fail closed, and `ModuleManager` plus direct module middleware enforce active, grace, expired, revoked, and invalid license outcomes.
- Product-license activation, verification failures, grace entry, expiry, and revocation now write idempotent append-only audit transitions with explicit state history; signed payloads and signatures are excluded, and revocation is atomic with its audit event.
- Core administrators now have permission-gated, read-only Filament visibility for product-license state, enabled modules, lifecycle dates, issuer key identity, and append-only audit history; signed payloads and signatures remain undisclosed, no mutation actions are registered, and UI state inspection performs no writes.
- Signed-license upgrades now use signed issue timestamps for deterministic precedence, replace matching license identities atomically only when newer, preserve existing entitlements on invalid or replayed replacements, and append non-secret upgrade audit events.
- SOP template versions now select the active department SOP workflow with global fallback, create ordered version-specific approval instances for every configured step, enforce role, department, mandatory ordering, author separation, and a different signer per decision, canonically sign approvals, rejections, and returns, expose a dedicated Template Approval Queue, and block publishing until every required signature verifies.
- SOP document approval authorization now requires a unique signer for every workflow step in addition to existing creator separation, role, department, mandatory ordering, and canonical signature enforcement.
- A central My Approval Queue now combines only currently actionable SOP document, SOP template, and enabled QMS deviation steps through an authorization-aware read model, links each item to its owning review surface, shows a combined dashboard preview and navigation badge, and hides the duplicate DMS queue navigation entries without moving decision or signature ownership out of their modules.
- Document printing presentation now hides direct Print/PDF actions unless a non-issuable document is approved or effective, keeps issuable controlled copies printable only from active issuance rows, and guides blocked direct URLs to the Log Documents issuance workflow without weakening controller, policy, entitlement, watermark, or audit enforcement.
- Controlled-copy issuance now serializes number allocation by locking the parent document, skips any already-reserved issuance number, revalidates eligibility inside the transaction, retries deadlocks, and converts residual uniqueness races into a user-facing validation message instead of an exception page.
- Phase 9 now has a dormant QMS-owned Change Control foundation with explicit lifecycle vocabulary, responsibility and milestone persistence, factories, and QMS-only permissions; no incomplete Filament workflow is exposed.
- QMS Change Control now implements the Shared approvable-subject boundary, and its unbound QMS submission-authorization adapter enforces permission, attribution, department scope, and explicit manager override without changing DMS workflow persistence.
- QMS Change Control now stores controlled-document impact actions and rationale with source/result document traceability and duplicate-action prevention, without executing document revisions.
- Approved QMS revise impacts can now create exactly one traced DMS draft revision through an entitlement- and permission-enforced atomic service; retries are idempotent and unapproved or non-revision impacts remain blocked.
- Change Control lifecycle mutations now run through a QMS entitlement- and permission-enforced transition service with explicit allowed states, milestone timestamps, actor/reason attribution, secret-sanitized context, and append-only audit events; document-revision implementation uses this same transition boundary.
- An entitlement- and policy-gated QMS Change Control Filament resource now supports attributed draft creation/editing, lifecycle visibility, submission through the transition service, and read-only audit history; disabled QMS and unpermitted direct routes are blocked.
- Change Control reviewers can now begin review, approve, reject, cancel, begin effectiveness review, and close records through permission-aware Filament actions that require attributable reasons and use the audited transition service exclusively.
- Draft Change Controls now manage controlled-document impacts through Filament, while approved revise impacts expose a permission-gated action that delegates to the atomic DMS revision service and records the resulting document trace.
- Consequential Change Control decisions now implement the Shared electronic-signature record boundary with canonical hashes over event identity, meaning, signer, timestamp, reason, IP address, and user agent; approval tampering is detectable while non-decision review events remain unsigned.
- Phase 10 now has a dormant QMS-owned Deviation foundation with explicit lifecycle and severity vocabulary, occurrence/discovery details, responsibility and due-date persistence, generated references, factories, and QMS-only permissions; no incomplete Filament workflow is exposed.
- Phase 10 now includes a dormant QMS Investigation record linked to Deviation with explicit lifecycle, lead ownership, methodology, root cause, conclusion, due dates, completion milestones, factories, and QMS-only permissions.
- Phase 10 now includes a dormant QMS CAPA record linked to Deviation and optional Investigation with corrective/preventive classification, action-plan lifecycle, ownership, completion and effectiveness milestones, factories, and QMS-only permissions.
- Deviation now implements the Shared approvable-subject boundary, and an unbound QMS submission-authorization adapter enforces permission, reporter/owner attribution, department scope, and manager override without changing DMS or Change Control workflow persistence.
- Deviation lifecycle mutations now run through a QMS entitlement- and permission-enforced transition service with append-only actor/reason history and Shared canonical signatures for investigation completion, rejection, cancellation, effectiveness review, and closure.
- Focused seeder, entitlement, and document-version tests passed at the checkpoint.
- CSV Validation reporting now reuses the system report-template registry and export pipeline for ALCOA+ requirement-to-risk/test/execution/evidence traceability in CSV and Excel, plus a printable GMP validation summary with signed lifecycle history; direct exports enforce QMS entitlement and project-view permission.

Always verify these statements against the worktree before relying on them.

## Ordered phases

1. **Entitlement foundation — completed**
   Centralize DMS, QMS, and AI enablement with dependency enforcement.

2. **Runtime enforcement — completed**
   Protect current DMS and AI routes, Filament surfaces, actions, jobs, and widgets.

3. **Permission and seeder split — completed**
   Extract legacy DMS permissions and role assignment from `SopModuleSeeder` into Core and DMS seeders. Keep AI and future QMS permissions in their module seeders. Test DMS-only and DMS+AI seeding.

4. **Domain boundaries — completed**
   Establish Shared, DMS, QMS, and AI namespaces incrementally. Avoid a large rename. Introduce generic controlled-document language before renaming persistent tables.
   - Slice 1 completed: Added the DMS `ControlledDocument` contract and adapted `SopDocument`.
   - Slice 2 completed: Moved document revision action and service ownership into the DMS domain while retaining legacy namespace compatibility.
   - Slice 3 completed: Moved document issuance lifecycle actions and service ownership into the DMS domain while retaining legacy namespace compatibility.
   - Slice 4 completed: Moved document and template retention lifecycle actions and service ownership into the DMS domain while retaining legacy namespace compatibility.
   - Slice 5 completed: Moved document locking actions and service ownership into the DMS domain while retaining legacy namespace compatibility.
   - Slice 6 completed: Moved template publishing action and service ownership into the DMS domain while retaining legacy namespace compatibility.
   - Slice 7 completed: Moved controlled-document generation action and service ownership into the DMS domain while retaining legacy namespace compatibility.
   - Slice 8 completed: Moved document numbering, SOP-reference resolution, and template-variable resolution into the DMS domain while retaining legacy namespace compatibility.
   - Slice 9 completed: Established canonical Shared ownership for audit logging while retaining the legacy SOP entry point and persistence.
   - Slice 10 completed: Moved approval-driven document activation into the DMS domain while retaining legacy namespace compatibility and supersession behavior.

5. **Reusable approval workflow — completed**
   Decouple approval subjects from `SopDocument` so controlled documents and future QMS records can share workflow infrastructure.
   - Slice 1 completed: Added the module-neutral Shared `ApprovableSubject` contract and adapted `SopDocument` without changing approval persistence.
   - Slice 2 completed: Decoupled workflow submission authorization from `SopDocument` by consuming `ApprovableSubject`, while retaining SOP workflow and approval persistence.
   - Slice 3 completed: Decoupled workflow selection from `SopDocument` by consuming `ApprovableSubject` department metadata, while retaining the existing SOP workflow persistence adapter.
   - Slice 4 completed: Added Shared workflow-definition and selector boundaries, adapted `SopWorkflow`, and moved department/global selection into the DMS adapter without changing SOP approval persistence.
   - Slice 5 completed: Adapted `SopWorkflowStep` to Shared step metadata and made workflow execution consume Shared workflow and step identities without changing `SopApproval`, document status, or audit persistence.
   - Slice 6 completed: Extracted approval-instance initialization behind a Shared persistence contract and retained `SopApproval` as the DMS adapter, including pending resets and duplicate prevention on resubmission.
   - Slice 7 completed: Adapted `SopApproval` to the Shared approval-instance contract without changing its table, relationships, authorization rules, decision methods, timestamp casts, or signature persistence.
   - Slice 8 completed: Made approve, reject, and return execution accept the Shared approval-instance contract while retaining an explicit `SopApproval` adapter guard and all existing decision behavior.
   - Slice 9 completed: Extracted approval decision mutations behind a Shared persistence contract while retaining `SopApproval` as the DMS adapter and preserving existing lookup, attribution, timestamp, comment, and signature behavior.
   - Slice 10 completed: Extracted approval decision authorization behind a Shared contract while retaining the DMS adapter's existing permission, role, department, ordering, and separation-of-duties behavior and the legacy model compatibility entry point.
   - Slice 11 completed: Extracted approval decision outcome orchestration behind a Shared contract while retaining SOP document statuses, audit events and payloads, mandatory completion checks, final activation, unlocking, and existing transaction boundaries.
   - Slice 12 completed: Extracted workflow submission lifecycle handling behind a Shared contract while retaining SOP draft validation, document unlocking, under-review status, submitted audit payloads, and the existing transaction boundary.
   - Slice 13 completed: Extracted workflow submission authorization behind a Shared contract while retaining SOP permissions, administrator and maker roles, department scope, creator and owner rules, and `canSubmit()` compatibility behavior.
   - Slice 14 completed: Removed the workflow engine's explicit `SopApproval` decision guard and return types, returning Shared approval instances while retaining DMS adapter validation and existing SOP action and Filament behavior.
   - Slice 15 completed: Introduced Shared approval decision-code vocabulary for workflow orchestration while retaining existing `approval_decisions` lookup rows, codes, foreign keys, DMS model constants, and string adapter boundaries.
   - Slice 16 completed: Moved canonical workflow-engine ownership into the Shared domain while retaining `App\Services\Sop\WorkflowEngineService` as a compatibility subclass and preserving existing action, Filament, and container-resolution behavior.

6. **Electronic signatures — completed**
   Add a shared attributable signable record with meaning, signer, timestamp, hash, reason, IP address, and user agent.
   - Slice 1 completed: Added the module-neutral Shared electronic-signature metadata contract and adapted existing SOP approval signature metadata without persistence changes.
   - Slice 2 completed: Added IP address and user-agent fields through an additive SOP approval migration, captured them for approval decisions through Shared orchestration, persisted them in the DMS adapter, and reset them on workflow reinitialization.
   - Slice 3 completed: Extracted electronic-signature hashing behind a Shared contract, added deterministic canonical SHA-256 hashing across all attributable metadata, and made approved SOP decisions use the new boundary with reproducible persisted hashes.
   - Slice 4 completed: Applied canonical Shared electronic signatures to rejected and returned decisions through the existing DMS actions while preserving status, audit, authorization, and rollback behavior.
   - Slice 5 completed: Added module-neutral electronic-signature record and verifier contracts, implemented canonical timing-safe integrity verification, and adapted persisted SOP approval decisions to the verification boundary.

7. **Module-aware presentation â€” completed**
   Separate DMS and QMS dashboards, settings, help, reports, navigation groups, and metrics.
   - Slice 1 completed: Established explicit DMS dashboard, navigation-group, and metric ownership, including enabled and disabled entitlement coverage, without introducing empty QMS surfaces.
   - Slice 2 completed: Moved Departments and Users into explicit Core organization and identity groups, made the remaining settings cluster and configuration sub-navigation DMS-owned, and verified disabled-module direct-route enforcement.
   - Slice 3 completed: Added product-module ownership to knowledge guides with a DMS-safe additive default, enum casting, entitlement-scoped resource queries and direct record resolution, enabled-only management choices, and module-aware table presentation.
   - Slice 4 completed: Added reusable product-module ownership and entitlement enforcement for reporting widgets, covering all existing DMS dashboard metrics and AI execution-performance metrics without coupling future QMS reports to either surface.

8. **License lifecycle — completed**
   Add signed activation, expiry, grace behavior, audit events, administrative visibility, and upgrade handling behind the existing entitlement contract.
   - Slice 1 completed: Added dormant signed-license persistence, trusted issuer-key configuration, and contract-bound RSA/SHA-256 verification with tamper, malformed-signature, unknown-key, persistence, and environment-authority coverage.
   - Slice 2 completed: Added a contract-bound activation service that verifies signatures before validating versioned license claims, enforces product-module dependencies, derives expiry and grace timestamps, and persists only valid licenses without changing active environment entitlements.
   - Slice 3 completed: Added a contract-bound lifecycle evaluator that re-verifies license signatures and deterministically classifies active, grace, expired, revoked, and invalid states at exact lifecycle boundaries while environment entitlements remain authoritative.
   - Slice 4 completed: Extracted environment-backed module normalization behind a replaceable entitlement-provider contract and made `ModuleManager` consume it while preserving dependency enforcement, middleware behavior, invalid-value filtering, and all existing consumers.
   - Slice 5 completed: Added an unbound signed-license entitlement provider that selects verified active or grace claims, supports safe fallback to an older usable license, enforces known unique dependency-complete modules, and fails closed for every unusable lifecycle or payload state while configuration remains authoritative.
   - Slice 6 completed: Added an explicit environment-or-signed-license provider switch with environment behavior unchanged by default, opt-in signed claims enforced through `ModuleManager` and route middleware, and fail-closed handling for expired, revoked, invalid, or unknown source states.
   - Slice 7 completed: Added a dedicated append-only license audit stream for activation, verification failure, grace, expiry, and revocation transitions; lifecycle evaluation is idempotent, secret material is excluded, and revocation persists atomically with its state event.
   - Slice 8 completed: Added permission-gated Core administrative visibility for license state, modules, lifecycle dates, issuer key identity, and audit history through index/view-only Filament surfaces, with no payload or signature exposure, no mutation actions, and pure read-only state resolution.
   - Slice 9 completed: Added signed issue-time persistence and deterministic license precedence, atomic same-identity upgrades, replay and downgrade rejection, uninterrupted fallback on invalid replacements, and explicit non-secret upgrade audit history.

9. **QMS Change Control — completed**
   Build the first QMS bounded context and connect approved change controls to controlled document revisions.
   - Slice 1 completed: Added the dormant QMS-owned Change Control status vocabulary, core record, responsibility and lifecycle schema, factory, no-op production seeder, and module-owned permissions without exposing Filament navigation or routes.
   - Slice 2 completed: Adapted Change Control to Shared approvable-subject identity, reference, title, department, requester, and owner metadata, and added an unbound QMS submission-authorization adapter with permission, attribution, department, and manager coverage while leaving DMS workflow persistence unchanged.
   - Slice 3 completed: Added QMS-owned controlled-document impact action vocabulary and persistence with source/result document relationships, rationale, parent traceability, and duplicate-action prevention without executing revisions.
   - Slice 4 completed: Added an entitlement- and permission-enforced QMS service that atomically turns an approved revise impact into one DMS draft revision, records the result trace, moves the change into implementation, and returns the existing revision on safe retries.
   - Slice 5 completed: Added append-only Change Control lifecycle audit events and an entitlement- and permission-enforced transition service covering allowed state changes, milestone timestamps, actor and reason attribution, secret-sanitized context, and immutable history; document-revision implementation now records its approved-to-implementing transition through the same boundary.
   - Slice 6 completed: Added an entitlement- and policy-gated QMS Change Control Filament resource with generated human references, attributed draft creation, draft-only editing, lifecycle visibility, transition-service submission, immutable audit history, and disabled-module plus unpermitted direct-route enforcement.
   - Slice 7 completed: Added permission-aware Filament lifecycle actions for review, approval, rejection, cancellation, effectiveness review, and closure; every action requires an attributable reason and delegates to the audited transition service.
   - Slice 8 completed: Added draft-only controlled-document impact planning in Filament and a permission-gated approved-revision action that delegates to the atomic DMS revision service, records the resulting draft, and moves the parent change into implementation.
   - Slice 9 completed: Applied Shared canonical electronic signatures to approval, rejection, cancellation, effectiveness-review, and closure events with signer, timestamp, reason, IP address, user agent, immutable record identity, and tamper verification.

10. **Deviation, Investigation, and CAPA â€” completed**
   Build linked quality-event lifecycles using shared workflow, signatures, attachments, audit, and effectiveness checks.
   - Slice 1 completed: Added the dormant QMS-owned Deviation status and severity vocabulary, core occurrence/discovery record, responsibility and due-date schema, generated references, factory, no-op seeder, and module-owned permissions without exposing Filament navigation or routes.
   - Slice 2 completed: Added the dormant QMS-owned Investigation lifecycle and record linked to Deviation, including lead responsibility, methodology, root cause, conclusion, due and completion milestones, generated references, factory, no-op seeder, and module-owned permissions.
   - Slice 3 completed: Added the dormant QMS-owned CAPA classification, lifecycle, and action-plan record linked to Deviation and optional Investigation, including ownership, due/completion milestones, effectiveness checks, generated references, factory, no-op seeder, and module-owned permissions.
   - Slice 4 completed: Adapted Deviation to Shared approvable-subject identity, reference, title, department, reporter, and owner metadata, and added an unbound QMS submission-authorization adapter with permission, attribution, department, and manager coverage.
   - Slice 5 completed: Added append-only Deviation lifecycle audit events and an entitlement- and permission-enforced transition service with explicit state rules, actor and reason attribution, secret-sanitized context, and Shared canonical signatures for consequential decisions.
   - Slice 6 completed: Added append-only Investigation lifecycle audit events and entitlement- and permission-enforced transitions with milestone attribution, completion validation, Shared canonical signatures, and a parent Deviation gate requiring every linked investigation to be completed.
   - Slice 7 completed: Added append-only CAPA lifecycle audit events and entitlement- and permission-enforced planning, implementation, effectiveness, rework, closure, and cancellation transitions with required effectiveness evidence, milestone attribution, Shared canonical signatures, and a parent Deviation gate requiring every linked CAPA to have a successful effectiveness decision.
   - Slice 8 completed: Added QMS-entitled and explicitly permission-gated Filament resources for Deviation, Investigation, and CAPA with attributable draft creation, state-aware editing, linked-record forms, transition-service-only lifecycle actions, read-only audit history, direct-route module middleware, and disabled-module plus unpermitted access coverage; the existing Change Control routes now use the same explicit QMS middleware.
   - Slice 9 completed: Added one private-by-default polymorphic quality-evidence boundary shared by Deviation, Investigation, and CAPA with attributable uploader and file metadata, immutable attachment records, permission- and entitlement-enforced Filament uploads, path-tampering prevention, temporary private downloads, and no production seed data.
   - Slice 10 completed: Added a module-neutral streaming SHA-256 content-integrity boundary, canonical digest capture for newly uploaded quality evidence, timing-safe verification with explicit verified, altered, missing, and legacy-unhashed states, Filament integrity visibility, and additive persistence compatible with existing attachments.
   - Slice 11 completed: Added QMS-owned generic quality workflow definitions, ordered role and department steps, and polymorphic approval cycles implementing Shared workflow contracts without using SOP workflow or approval tables; Deviation submission now creates configured department/global pending approvals atomically, retains the existing direct-submission fallback when no workflow is configured, exposes read-only workflow progress, preserves prior signed cycles on resubmission, and remains entitlement- and attribution-enforced.
   - Slice 12 completed: Extracted module-neutral Shared decision orchestration and added QMS decision adapters with latest-cycle and mandatory-step ordering, role and department scope, separation of duties, lifecycle authority, row-locked persistence, canonical electronic signatures, timing-safe verification, transactional approval/rejection/return outcomes, signed Deviation audit transitions, terminal-step disposition, and permission-gated Filament actions while retaining all existing DMS workflow behavior.

11. **Remaining QMS and validation — completed**
    Add complaints, audits, risk management, supplier quality, management review, reports, security regression tests, and release validation.
   - Slice 1 completed: Added the dormant QMS-owned Complaint source, type, and lifecycle vocabulary; attributable intake, ownership, product and batch traceability, nullable safety and regulatory-assessment fields, reporting and response milestones, generated references, factory, no-op production seeder, and module-owned permissions without exposing Filament navigation or routes.
   - Slice 2 completed: Added append-only Complaint lifecycle audit events and an entitlement- and permission-enforced transition service with explicit intake, assessment, investigation, response, closure, rejection, and cancellation rules; attributable reasons on every transition; regulatory-assessment and reporting-evidence closure gates; sanitized context; canonical electronic signatures for consequential decisions; and tamper verification without exposing incomplete navigation.
   - Slice 3 completed: Added nullable unique Complaint-to-Deviation source traceability and an entitlement- plus dual-permission-enforced handoff service that atomically creates one retry-safe Deviation from an assessed product-quality complaint, preserves responsibility and investigation context, advances the Complaint through its audited lifecycle, and prevents later source reassignment.
   - Slice 4 completed: Registered Complaint records with the existing private polymorphic quality-evidence boundary so complaint evidence inherits immutable metadata, attributable upload identity, path isolation, streaming SHA-256 capture, and verified, altered, missing, or legacy-unhashed integrity states while the incomplete Complaint UI remains dormant.
   - Slice 5 completed: Added the dormant QMS-owned Internal Audit type and lifecycle vocabulary; scope, objectives, and criteria; schedule, execution, reporting, follow-up, and closure milestones; department, lead-auditor, creator, and owner attribution; generated references, factory, no-op production seeder, and module-owned permissions without exposing Filament navigation or routes.
   - Slice 6 completed: Added append-only Internal Audit lifecycle events and an entitlement- and permission-enforced transition service with explicit scheduling, conduct, reporting, follow-up, closure, and cancellation rules; required attributable reasons; validated lead, schedule, issued-report, and follow-up milestones; automatic execution/completion/closure timestamps; sanitized context; canonical electronic signatures for consequential decisions; and tamper verification without exposing incomplete navigation.
   - Slice 7 completed: Added dormant Internal Audit Finding severity, classification, and disposition vocabulary; audit, clause, and objective-evidence traceability; department, owner, identifier, and verifier attribution; response, verification, and closure milestones; generated references, factory, no-op production seeder, and module-owned permissions without exposing Filament navigation or routes.
   - Slice 8 completed: Added append-only Audit Finding disposition events and an entitlement- and permission-enforced transition service with explicit response request, submitted-response verification, independent acceptance, signed rework, rejection, closure, and cancellation rules; required attributable reasons and response/verification evidence; enforced owner-verifier separation; preserved rejected verification evidence in immutable history; and applied canonical signatures plus tamper verification without exposing incomplete navigation.
   - Slice 9 completed: Added nullable unique Audit Finding-to-CAPA source traceability, made the existing CAPA aggregate support exactly one immutable Deviation or Audit Finding source, and added an entitlement- plus dual-permission-enforced handoff service that atomically creates one retry-safe CAPA from an open actionable nonconformity, preserves finding context in append-only history, validates ownership and effectiveness timing, and retains all existing CAPA lifecycle behavior.
   - Slice 10 completed: Added an Internal Audit closure gate that prevents signed closure while any linked finding remains open, response-pending, under verification, or accepted-but-not-closed; permits closure only when every finding is closed, rejected, or cancelled; and preserves existing no-finding closure behavior.
   - Slice 11 completed: Registered Internal Audit and Audit Finding records with the existing private polymorphic quality-evidence boundary so audit plans, reports, objective evidence, responses, and verification records inherit immutable metadata, attributable upload identity, path isolation, streaming SHA-256 capture, and verified, altered, missing, or legacy-unhashed integrity states while incomplete audit UI remains dormant.
   - Slice 12 completed: Added the dormant QMS-owned Risk Assessment type and lifecycle vocabulary; scope, hazard, harm, and existing-control traceability; department, owner, creator, and approver attribution; separately persisted initial and residual severity, probability, and detectability factors with consistent derived RPN calculations and unknown residual state; mitigation, review, approval, and closure milestones; generated references, factory, no-op production seeder, and module-owned permissions without exposing Filament navigation or routes.
   - Slice 13 completed: Added append-only Risk Assessment lifecycle events and an entitlement- and permission-enforced transition service with explicit review, independent approval/rejection, mitigation, monitoring, closure, cancellation, and monitoring-rework rules; enforced 1–5 initial and residual scoring bounds, creator/owner approval separation, mitigation evidence, complete residual scoring, scheduled review, and non-increasing residual RPN at closure; and applied attributable reasons, canonical signatures, sanitized context, and tamper verification without exposing incomplete navigation.
   - Slice 14 completed: Registered Risk Assessment records with the existing private polymorphic quality-evidence boundary so hazard analyses, scoring rationale, mitigation evidence, and residual-risk verification inherit immutable metadata, attributable upload identity, path isolation, streaming SHA-256 capture, and verified, altered, missing, or legacy-unhashed integrity states while incomplete Risk Assessment UI remains dormant.
   - Slice 15 completed: Added the dormant QMS-owned Supplier Qualification category, risk, and lifecycle vocabulary; legal and site identity, material/service scope, country and contact traceability; department, owner, creator, and approver attribution; qualification rationale, audit scheduling/completion, qualification, expiry, periodic review, suspension, and disqualification milestones; generated references, factory, no-op production seeder, and module-owned permissions without exposing Filament navigation or routes.
   - Slice 16 completed: Added append-only Supplier Qualification lifecycle events and an entitlement- and permission-enforced transition service with explicit assessment, audit-required, qualification/conditional qualification, suspension, signed reinstatement assessment, disqualification, expiry, and cancellation rules; enforced high/critical-risk audit evidence, qualification rationale, coherent review/expiry dates, creator/owner decision separation, and elapsed expiry; and applied attributable reasons, canonical signatures, sanitized context, and tamper verification without exposing incomplete navigation.
   - Slice 17 completed: Registered Supplier Qualification records with the existing private polymorphic quality-evidence boundary so questionnaires, certifications, audit reports, qualification rationale, suspension evidence, and periodic reviews inherit immutable metadata, attributable upload identity, path isolation, streaming SHA-256 capture, and verified, altered, missing, or legacy-unhashed integrity states while incomplete Supplier Quality UI remains dormant.
   - Slice 18 completed: Added the dormant QMS-owned Management Review type, lifecycle, and required quality-system input vocabulary; review-period and meeting scheduling; chair, coordinator, creator, and approver attribution; typed required-input selection, input summary, decisions, and action-summary outputs; minutes, approval, and completion milestones; generated references, factory, no-op production seeder, and module-owned permissions without exposing Filament navigation or routes.
   - Slice 19 completed: Added append-only Management Review lifecycle events and an entitlement- and permission-enforced transition service with explicit scheduling, conduct, minutes preparation, action follow-up, signed rework, dual-authority approval/completion, and cancellation rules; enforced coherent periods and meeting dates, chair/coordinator attribution, complete required-input category coverage, documented input/decision/action outputs, issued minutes, and creator/coordinator approval separation; and applied attributable reasons, canonical signatures, sanitized context, and tamper verification without exposing incomplete navigation.
   - Slice 20 completed: Registered Management Review records with the existing private polymorphic quality-evidence boundary so agendas, required-input packages, presentations, attendance records, controlled minutes, decisions, and action evidence inherit immutable metadata, attributable upload identity, path isolation, streaming SHA-256 capture, and verified, altered, missing, or legacy-unhashed integrity states while incomplete Management Review UI remains dormant.
   - Slice 21 completed: Added a QMS-entitled and explicitly `View:QualityMetrics`-gated aggregate reporting read model covering lifecycle counts and overdue workload for Deviation, Investigation, CAPA, Complaint, Internal Audit, Audit Finding, Risk Assessment, Supplier Qualification, and Management Review; excluded terminal records from overdue totals and returned only integer aggregates without titles, descriptions, contact details, user identity, or incomplete navigation.
   - Slice 22 completed: Added a cross-context QMS security regression matrix covering all ten lifecycle transition services, Complaint-to-Deviation and Audit-Finding-to-CAPA handoffs, aggregate metrics, and the shared evidence boundary; proved fail-closed behavior with QMS disabled or permissions absent, append-only update/delete rejection across all ten event streams, canonical signature validity before tampering, stored-record tamper detection afterward, and aggregate-report narrative exclusion.
   - Slice 23 completed: Performed the Phase 11 release-validation audit; replayed every migration from an empty dedicated testing database; ran the DMS and QMS module seeders twice without duplicate permissions or grants; prevented the development bootstrap administrator with its known password from being created outside local/testing environments; verified module-disabled and permission-denied boundaries, route and navigation exposure, dormant incomplete QMS surfaces, schema and append-only constraints, signature tamper detection, aggregate-report data minimization, and absence of debug residue; applied Pint formatting; and passed the complete 664-test, 2,715-assertion suite.

12. **Computer System Validation (CSV) — in progress**
    Add a risk-based validation workspace covering intended use, GxP assessment, requirements, specifications, risks, test protocols and executions, deviations, traceability, release, and periodic review.
   - Slice 1 completed: Added QMS-owned validation projects with system identity, intended use, GxP criticality, regulatory scope, electronic-record/signature applicability, responsible owners, validation strategy, controlled-document links, release baseline, summary, review scheduling, and generated project identity.
   - Slice 2 completed: Added versioned requirements, risk assessments, specifications, test cases, test executions, requirement-to-test traceability, deviation links, periodic reviews, and immutable project lifecycle events.
   - Slice 3 completed: Added an entitlement- and permission-enforced lifecycle service with explicit phase transitions, signed consequential decisions, independent QA release authority, approved requirement/specification gates, accepted residual-risk gates, and independently reviewed passing-test traceability for GxP and high/critical requirements.
   - Slice 4 completed: Added a QMS-entitled Filament validation workspace with project readiness visibility and project-contained requirements, risks, specifications, trace-linked tests, execution records, periodic reviews, and read-only signed history; deletion is disabled and released/retired records are protected from editing.
   - Slice 5 completed: Added module permissions, migrated the new schema into the unused application database, seeded the administrator role, verified route discovery, and added focused release-gate, separation-of-duties, signature-integrity, and missing-evidence tests.
   - Slice 6 completed: Corrected the Filament lifecycle action boundary so failed CSV transitions halt safely and display a persistent notification containing every unmet validation gate; QA Release no longer appears unresponsive when the signed release package is incomplete.
   - Slice 7 completed: Extended the report-template scopes and field registry with GMP/ALCOA+ CSV Validation traceability and validation-summary definitions; seeded system-standard traceability CSV/Excel and printable-summary templates; added template-selectable, QMS-entitled and permission-gated exports; and verified configured column order, linked risk/test/execution/evidence/review values, spreadsheet response metadata, printable summary fields, and disabled-module direct access.
   - Remaining: Move requirement/specification/test approval and test-execution review into dedicated signed decision services, add immutable evidence uploads to executions, enforce failed-test deviation resolution, and perform full regression and empty-database release validation.

13. **Core organization profile — completed**
    Store the deployment owner's legal-entity identity once and apply it consistently to every controlled document.
   - Slice 1 completed: Added a Core-owned organization profile with normalized organization code and country, legal/display names, company and tax registration, extensible regulatory identifiers, registered address, contact details, branding, timezone, active/default state, and creator attribution.
   - Slice 2 completed: Added permission- and policy-gated Filament create/view/edit surfaces under Core Organization, enforced one profile per individual company deployment, disabled deletion and create-another behavior, and labelled the navigation item Organization Profile.
   - Slice 3 completed: Removed organization selection and filtering from transactional document screens; controlled-document generation now resolves the deployment profile automatically and stores an immutable full identity snapshot on every generated document and revision.
   - Slice 4 completed: Added reusable `organization.*` template placeholders plus snapshot-based print/PDF identity, logo, registration, address, header, and footer rendering so later organization edits cannot alter historical document output.
   - Slice 5 completed: Applied the additive migrations and Core/DMS permissions, verified Filament route discovery, and passed focused organization/default/snapshot/placeholder tests plus document-generation and printing regressions.

## Immediate next task

Complete the CSV signed sub-record decisions, evidence and deviation gates, and full release-validation pass before describing the application as a turnkey CSV solution.
